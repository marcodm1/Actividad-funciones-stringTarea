{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "El puto debugguer",
            "type": "php",
            "request": "launch",
            "program": "${file}",
            "cwd": "${fileDirname}",
            "port": 9000
        }
    ]
}